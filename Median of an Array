class Solution {
	public double findMedian(int[] arr) {
		// Code here.
		int n = arr.length;
		Arrays.sort(arr);
		if (n % 2 != 0) return arr[n / 2];
		
		return (arr[(n - 1) / 2] + arr[n / 2]) / 2.0;
	}
}
